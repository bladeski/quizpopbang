# Exported from Render on 2024-12-03T15:51:58Z
services:
- type: web
  name: quizpopbang
  runtime: static
  repo: https://github.com/bladeski/quizpopbang
  envVars:
  - key: PUBLIC_WS_PORT
    sync: false
  - key: PUBLIC_WS_URL
    sync: false
  - key: PUBLIC_PORT
    sync: false
  - key: PUBLIC_BASE_URL
    sync: false
  buildCommand: yarn --frozen-lockfile install; yarn build
  staticPublishPath: dist
version: "1"
